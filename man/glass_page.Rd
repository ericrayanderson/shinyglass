% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass-page.R
\name{glass_page}
\alias{glass_page}
\title{Create a Liquid Glass Shiny page}
\usage{
glass_page(
  ...,
  title = "",
  header = NULL,
  theme = glass_theme(),
  fillable = FALSE
)
}
\arguments{
\item{...}{UI elements to place in the page body.}

\item{title}{Page title shown in the browser tab.}

\item{header}{Optional header content (e.g. a title string or tag). When a
character string, it is rendered in a glass navigation bar.}

\item{theme}{A [bslib::bs_theme()] object. Defaults to [glass_theme()].}

\item{fillable}{Whether the page should fill the viewport height.}
}
\value{
A Shiny UI definition.
}
\description{
Convenience wrapper around [bslib::page_fluid()] with [glass_theme()]
pre-applied. For existing apps, prefer passing `theme = glass_theme()` to
your existing page function instead.
}
\examples{
\dontrun{
library(shiny)
library(shinyglass)

ui <- glass_page(
  header = "My Dashboard",
  glass_card(
    h3("Hello"),
    p("Liquid glass styling applied automatically.")
  )
)
}

}
