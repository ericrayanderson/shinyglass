% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass-page.R
\name{glass_page}
\alias{glass_page}
\title{One-call glass page}
\usage{
glass_page(
  ...,
  title = NULL,
  preset = "auto",
  intensity = 0.45,
  persist = TRUE,
  scene = "tahoe",
  wallpaper = NULL,
  controls = TRUE,
  theme = NULL
)
}
\arguments{
\item{...}{UI elements passed to \code{\link[shiny:fluidPage]{shiny::fluidPage()}}.}

\item{title}{Optional page title (\code{\link[shiny:titlePanel]{shiny::titlePanel()}}).}

\item{preset, intensity, persist, scene, wallpaper}{Forwarded to \code{\link[=glass_theme]{glass_theme()}}.
Persistence defaults to \code{TRUE} here.}

\item{controls}{\code{TRUE} for the default control row (toggle, intensity,
accent), \code{FALSE} for none, or a character vector subset of
\code{"toggle"}, \code{"intensity"}, \code{"accent"}.}

\item{theme}{Optional pre-built \code{\link[=glass_theme]{glass_theme()}} object. When \code{NULL}, one is
created from the arguments above.}
}
\value{
A Shiny UI tag list suitable as \code{shinyApp(ui = ...)}.
}
\description{
Wraps \code{\link[shiny:fluidPage]{shiny::fluidPage()}} with \code{\link[=glass_theme]{glass_theme()}} and, by default, the Light /
Dark / Auto toggle, intensity slider, and accent wells. Pair with
\code{\link[=observe_glass]{observe_glass()}} in the server function.
}
\examples{
if (interactive()) {
  library(shiny)
  library(shinyglass)

  ui <- glass_page(
    title = "Hello, glass",
    plotOutput("plot")
  )
  server <- function(input, output, session) {
    observe_glass(input, session)
    output$plot <- renderPlot(plot(1:10), bg = "transparent")
  }
  shinyApp(ui, server)
}
}
