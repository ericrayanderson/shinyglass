% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass-flatten.R
\name{glass_flatten}
\alias{glass_flatten}
\title{Flatten glass for print, PDF, and screenshots}
\usage{
glass_flatten(session, on = TRUE)
}
\arguments{
\item{session}{A Shiny session object.}

\item{on}{\code{TRUE} to flatten, \code{FALSE} to restore live glass.}
}
\value{
\code{session}, invisibly.
}
\description{
Neutralizes backdrop blur and translucent fills so chromote, \code{pagedown},
print-to-PDF, static HTML, and shareable shots stay readable. The live
page can enter or leave flatten without a reload.
}
\details{
Ways to enter flatten:
\itemize{
\item \code{\link[=glass_theme]{glass_theme()}} \code{flatten = TRUE} (first paint)
\item \code{\link[=glass_flatten]{glass_flatten()}} / \code{\link[=update_glass_theme]{update_glass_theme()}} \code{flatten = TRUE} (live)
\item \code{window.shinyglass.setFlatten(true)} (or \code{.enterFlatten()} / \code{.exitFlatten()})
\item Query string \code{?glass_flatten=1} (handy for chromote / visual QA)
\item \verb{@media print} (automatic for browser print / PDF)
}
}
\examples{
if (interactive()) {
  library(shiny)
  library(shinyglass)

  ui <- fluidPage(
    theme = glass_theme(),
    actionButton("flat", "Flatten for capture"),
    actionButton("live", "Live glass")
  )
  server <- function(input, output, session) {
    observeEvent(input$flat, glass_flatten(session, TRUE))
    observeEvent(input$live, glass_flatten(session, FALSE))
  }
  shinyApp(ui, server)
}
}
\seealso{
\code{\link[=update_glass_theme]{update_glass_theme()}}, \code{\link[=glass_theme]{glass_theme()}}
}
